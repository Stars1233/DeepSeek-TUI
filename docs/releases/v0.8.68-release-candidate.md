# Codewhale v0.8.68 Release-Candidate Ledger

> **Authoritative status as checked 2026-07-15:** release source under final
> exact-HEAD verification; not yet publicly published. Update this ledger when
> the candidate PR, verification, tag, or package state changes. Historical
> trackers and handoffs are planning evidence only.

## Candidate identity

| Surface | Current state |
|---|---|
| Workspace Cargo version | `0.8.68` |
| npm wrapper manifest | `codewhale@0.8.68` |
| Candidate branch | `codex/v0868-cursor-qa-fixes` |
| Pre-integration audit HEAD | `508b8b80b` (before custom-provider and locale-name integration) |
| GitHub `v0.8.68` tag | Absent |
| GitHub `v0.8.68` release | Absent |
| crates.io `codewhale-*` release set | `0.8.68` absent; published crates remain at `0.8.67`, while `codewhale-lane` has no public version yet |
| npm `codewhale` | `0.8.68` absent; latest is `0.8.67` |

`scripts/release/check-published.sh 0.8.68`, the GitHub release API, and the
remote tag list were checked on 2026-07-15. A dated changelog section and local
version metadata do not make a public release.

## Acceptance accounting

GitHub issues stay open until the candidate is pushed, reviewed, and its final
evidence is attached. As of the 2026-07-15 refresh, the candidate contains
fixes and regression coverage for the bounded `@` discovery report (#4365),
Kimi's false 1K compaction warning (#4368), functional Simplified Chinese
setup terminology (#4369), and approval-time transcript review (#4371).
Named custom-provider identity (#4334) is release-blocking until the exact
final patch proves built-in, named-custom, legacy-literal, removed-route,
reload, Fleet receipt, and runtime-thread behavior without persisting secrets.

The following work is not honestly closed by this candidate audit:

- [#4175](https://github.com/Hmbown/CodeWhale/issues/4175),
  [#4177](https://github.com/Hmbown/CodeWhale/issues/4177),
  [#4178](https://github.com/Hmbown/CodeWhale/issues/4178), and
  [#4179](https://github.com/Hmbown/CodeWhale/issues/4179) still require
  completed live Fleet role-resolution, stopship-dogfood, and gate/handoff
  receipts before their broader acceptance can close.
- [#4236](https://github.com/Hmbown/CodeWhale/issues/4236) and
  [#4242](https://github.com/Hmbown/CodeWhale/issues/4242) retain official
  Termux scope and real-device runtime QA.

The same refresh found #4372 safe and fully green; it was merged to `main` as
`9035890c4`, preserving @nightt5879's original commit and @CCChisato's
co-author credit for #3915. The seven remaining open pull requests are not safe
release merges: #4370 is a conflicting, provider-specific slice of unfinished
generic catalog work; #4367 is replaced by the bounded off-thread mention fix
in this candidate; and #4293, #4199, #4087, #4086, and #3781 remain explicitly
deferred, draft, conflicting, or outside v0.8.68 scope. These decisions must be
rechecked immediately before the release PR merges.

Provider-route degradation, a missing Fleet terminal receipt, or a role/gate
failure is a failed release gate, not an external/manual gap. The only accepted
external platform gap is the explicitly documented real-device Android/Termux
runtime pass; the target remains preview-only until that evidence exists.

## Verification and publication gates

Earlier metadata checks and Cargo dry-runs predate the final integration and do
not count as exact-HEAD evidence. Before calling v0.8.68 ready or published,
complete all of the following against the final commit:

1. Run `scripts/release/check-versions.sh`,
   `scripts/release/check-ohos-deps.sh`, changelog synchronization checks,
   `cargo fmt --all -- --check`, and `git diff --check`.
2. Run the locked workspace `check`, strict all-target/all-feature `clippy`,
   full workspace tests, the TUI PTY/runtime lifecycle gates, and every focused
   stopship regression added during this integration.
3. Run `scripts/release/publish-crates.sh dry-run`, build the CLI and TUI with
   `--release --locked`, and smoke the npm wrapper against those artifacts.
4. Install that exact commit with `scripts/release/install-dogfood.sh`; verify
   all three global entry points report the same version and commit. Exercise
   the underwater shell in Cursor at the release terminal sizes, including
   Operate messaging/tools, Ask approvals, approval-time transcript review,
   settings, routes, links, Fleet setup/steering/cancellation, locale-aware
   worker names, reduced motion, and custom-provider save/resume/fail-closed
   behavior.
5. Run the real tmux stopship Workflow with the release Fleet and 360k aggregate
   budget. Preserve role-resolved child, gate, handoff, and terminal receipts
   for #4175/#4177/#4178/#4179; a partial run does not pass.
6. Push the candidate, open and review a PR against `main`, wait for required
   CI, merge, and verify the merge commit before updating issue accounting.
7. Create `v0.8.68` either through the repository's release-tag workflow or a
   signed local tag. The workflow path creates an unsigned lightweight tag, so
   verify the tag and GitHub release resolve to the intended `main` commit
   instead of describing that path as signed.
8. Wait for release assets and run
   `scripts/release/verify-release-assets.sh 0.8.68` **before** publishing the
   ordered Cargo crate set and `codewhale@0.8.68` to npm. Require all 29 public
   assets, both checksum manifests, the matching successful workflow SHA, and
   the exact clean tag-checkout guard. `codewhale-lane` is new on crates.io in
   this release; its first publish is an authentication stop line if the token
   cannot create a crate.
9. Run `scripts/release/check-published.sh 0.8.68` and verify GitHub checksums,
   GHCR, CNB, Cargo installs, npm wrapper resolution, release-note contributor
   credit, and registry parity. `deepseek-tui` remains deprecated/unpublished;
   the experimental runtime SDK/plugin is not part of this release.

After publication, update this ledger with the final source SHA, release URL,
publication date, and registry checks. Do not install or publish the local
experimental Z.AI plugin draft as part of v0.8.68; it is outside this candidate
and has not passed the release gates.
